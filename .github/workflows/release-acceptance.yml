name: Hello World Workflow

on:
  push:
    branches:
      - 2037-release-acceptance-test-workflow

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"
