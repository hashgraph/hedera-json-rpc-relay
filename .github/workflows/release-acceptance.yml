name: Release Acceptance Test Workflow

on:
  push:
    branches:
      - 2037-release-acceptance-test-workflow

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello Acceptance Test
        run: echo "Hello Acceptance Test"
