# Always comment on PRs
comment:
  layout: "diff, flags, files" # delete old and post new
  behavior: new
  require_changes: false

coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%